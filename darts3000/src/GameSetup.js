import React, { useState } from 'react';

// maybe create a big object (?) with all the details and variables needed for a game (startingscore, finishingmode, players, time, gameWon, Date and Time, scores,...)
//   const handleNextTurn = () => {
//    const nextPlayerIndex = (currentPlayerIndex + 1) % players.length;
//   setCurrentPlayerIndex(nextPlayerIndex);